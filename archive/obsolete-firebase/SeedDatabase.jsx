// Archived: SeedDatabase.jsx (migration UI to Firestore). Kept for reference.
// The live application no longer uses Firestore; migrations were moved to MySQL.
import React from 'react';
export default function SeedDatabaseArchived() {
    return (
        <div style={{padding:20}}>
            <h2>SeedDatabase (archived)</h2>
            <p>This page was archived. Migrations should run server-side against MySQL now.</p>
        </div>
    );
}
