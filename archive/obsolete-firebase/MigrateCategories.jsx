// Archived MigrateCategories page (was migrating Firestore categories). Kept for reference.
import React from 'react';
export default function MigrateCategoriesArchived() {
    return (
        <div style={{padding:20}}>
            <h2>MigrateCategories (archived)</h2>
            <p>This migration UI was archived — migrations should be run server-side against MySQL.</p>
        </div>
    );
}
